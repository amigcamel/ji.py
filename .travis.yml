language: python
python:
  - "3.6"
  - "3.7"
dist: xenial
sudo: true
install:
  - pip install tox-travis flake8 flake8-docstrings pytest pytest-cov python-coveralls
script:
  - tox 
after_success:
  - coveralls

